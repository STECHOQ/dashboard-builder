{
  "name": "dashboard",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "start": "./start",
    "build": "./build",
    "preview": "vite preview --host --port 3140"
  },
  "devDependencies": {
    "vite": "^7.0.4"
  }
}
